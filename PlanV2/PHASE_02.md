# Phase 2 — SQLite DB & DPAPI Vault

## Goal
Establish the data layer using Entity Framework Core with SQLite, and build the credential vault encrypted with Windows DPAPI.

## Scope
- `DbContext` setup.
- Vault service for storing credentials.
