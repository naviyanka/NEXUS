# Phase 1 — .NET 8 Gateway & Auth

## Goal
Set up the core ASP.NET Core 8 minimal API and configure `Microsoft.AspNetCore.Authentication.Negotiate` for Kerberos/NTLM authentication.

## Scope
- Implement authentication middleware.
- Create base API routing structure.
