# Phase 5 — Plugin Loader Engine

## Goal
Build the system that dynamically discovers, loads, and initializes NEXUS plugins from external folders.

## Scope
- Plugin manifest parsing (`plugin.json`).
- Dynamic route registration.
